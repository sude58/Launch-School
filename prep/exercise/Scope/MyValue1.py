if True:
    my_value = 20

print(my_value) # Will print 20 since variable initailized in block can be accessed outside of that block. It will raise NameError if it had False instead of True since the block will never reach.